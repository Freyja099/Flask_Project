alembic==1.15.2   # Outil de migrations
aniso8601==10.0.0
blinker==1.9.0   # Système de signaux pour les notifications
cachelib==0.13.0
click==8.1.8
colorama==0.4.6
Flask==3.1.0
Flask-Caching==2.3.1  # Système de cache (mémoire, Redis, etc.)
Flask-Migrate==4.1.0  # Gestion des migrations de base de données (Alembic intégré)
Flask-RESTful==0.3.10 # Pour créer des APIs REST
Flask-SQLAlchemy==3.1.1 # Intégration ORM avec la base de données
greenlet==3.1.1  # Support pour le parallélisme 
itsdangerous==2.2.0 # Signatures cryptographiques 
Jinja2==3.1.6   # Moteur de templates
Mako==1.3.9      # Autre moteur de templates 
MarkupSafe==3.0.2  # Sécurisation des sorties HTML
python-dotenv==1.1.0
pytz==2025.2
six==1.17.0
SQLAlchemy==2.0.39   # ORM principal 
typing_extensions==4.13.0
watchdog==6.0.0
Werkzeug==3.1.3

# After using docker that's all I need

# flask
# flask-sqlalchemy
# werkzeug
# python-dotenv
# gunicorn